yg diubah:
1. menambahkan deteksi negatif dan tanda minus di itoa
2. menambahkan keyword val (konstanta) dan pembuatannya di statement parser dan codegen
3. memperbaiki handle assign saat reassign dengan variabel yang sama x = x + 1
4. memodularkan library stdio.asm yang awalnya berisi semua hal menjadi array, types, memory, stirng
5. mengubah folder stdlib menjadi runtime dan mengeluarkan folder std ke root
6. mengubah cara run dari ./run_fs path_file.ak menjadi aiko path_file.ak
7. menambahkan tipe array di typeof
8. mengubah implementasi prin array menjadi menampilkan semua
9. mengubah impplementasi pengecekan array print ke runtime hingga skrg array itu by ref
10. menambahkan keyword baru array untuk buat array dengan ukuran statis dengan default value 0 dengna menambahh di lexer parser dan codegen
11. menambah keyword push dan pop array, tpi implementasinya masih salah karna nanti akan diperbaiki saat memori array sudah diperbaiki agar bisa menyimpan kapasitas
12. menambahkan keyword baru null dengan type 9
13. untuk binary op memindahkan ke dalam operators asm agar lebih enak dilihat
14. menambah keyword view dilexer serta parsingnya di unary

yg belum:
1. ptr
2. perbaiki push pop
3. bug print(null == !false);
4. ubah jdi subroutine pengecekan for agar ga panjang
5. fitur cek akses angka atau stirng di generate array access pisah jdi macro bisa ndk si
6. kalau mau tambahin push pop dann capacity di array jd ga cuma length
7. untuk array offset bisa dibuat dalam section data untuk label, misal daripada [x + 88] lebih baik buat dlu .data + 88 itu misal INDEX_5 gitu
pengecekan ubah ke baris dan statement, kalau true keduanya baru return asm perbaris. btw yg diklik tu kurung kurawal untuk for dan function, ttik  koma untuk statement
8:
val hari = 3;
match hari {
    1 => print("Senin"),
    2 => print("Selasa"),
    3 => print("Rabu"),
    _ => print("Hari tidak valid") // _ adalah default/else
}